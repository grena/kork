{% extends 'base.html.twig' %}

{% block body %}
    <div class="block">
        <p class="small">{{ app.user.username }} - <a href="{{ path('logout') }}">{{ 'kork.global.logout'|trans }}</a></p>

        <a href="{{ url('join_random_game') }}" class="btn btn-inline">{{ 'kork.lobby.join_random_game'|trans }}</a>
    </div>
{% endblock body %}
